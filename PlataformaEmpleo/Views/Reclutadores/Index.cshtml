@model IEnumerable<PlataformaEmpleo.Models.Reclutador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEmpresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CorreoEmpresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEmpresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CorreoEmpresa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdReclutador">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdReclutador">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdReclutador">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
